import React from 'react';
import classes from './Home.module.css';

const home = (props) => (
  <div>
    <h1 className={classes.Header}>Investment Portfolio Home</h1>
  </div>
);

export default home;
